contexts:
  - id : Default
    mappings:
      - raw_type : Key
        raw_args :
          action : Release
          keycode : Escape
        action : Exit
      - raw_type : Key
        raw_args :
          keycode : W
        action : MoveForward
      - raw_type : Button
        raw_args:
          button: 1
          action: Release
        action : FireAbility1
      - raw_type : Motion
        raw_args : {}
        action : RotateDirection